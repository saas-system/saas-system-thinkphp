/**
 * 通常将表格的控制器URL定义在此处
 */

const base = '/tenant';

export const authMenu = base + '/auth.rule/'
export const authAdmin = base + '/auth.admin/'
export const authAdminLog = base + '/auth.adminLog/'
export const authGroup = base + '/auth.group/'
export const routineAttachment = base + '/routine.attachment/'

